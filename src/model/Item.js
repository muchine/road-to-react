class Item {
    constructor(id, title, author, commentCount, points) {
        this.id = id;
        this.title = title;
        this.author = author;
        this.commentCount = commentCount;
        this.points = points;
    }
}

export default Item;